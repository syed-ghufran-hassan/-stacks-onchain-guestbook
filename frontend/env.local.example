# ── Network selection ─────────────────────────────────────────────────────────
# Controls which Stacks network the frontend connects to.
# Options: devnet | testnet | mainnet
# Default: devnet (used by `stacksdapp dev`)
NEXT_PUBLIC_NETWORK=devnet

# ── Deployer private key (testnet / mainnet only) ─────────────────────────────
# Required for `stacksdapp deploy --network testnet` or --network mainnet.
# Never commit this file with a real key — .env.local is in .gitignore.
# DEPLOYER_PRIVATE_KEY=your_private_key_hex

# ── Stacks node overrides (optional) ─────────────────────────────────────────
# By default the CLI uses:
#   devnet  → http://localhost:3999
#   testnet → https://api.testnet.hiro.so
#   mainnet → https://api.hiro.so
# Uncomment to override:
# NEXT_PUBLIC_STACKS_NODE_URL=https://api.testnet.hiro.so

# ── Hiro API key (optional, recommended for testnet/mainnet) ─────────────────
# Used for rate-limited Hiro endpoints, including read-only calls and tx polling.
# NEXT_PUBLIC_HIRO_API_KEY=your_hiro_api_key

# ── Vercel deployment ─────────────────────────────────────────────────────────
# In Vercel Project Settings, set Root Directory to `frontend` (not the repo root).
# The included frontend/vercel.json sets framework to Next.js — do not override
# Output Directory to `public`; Next.js builds to `.next` automatically.
